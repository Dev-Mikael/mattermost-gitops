# apps/mattermost/secrets/sealed-db-credentials.yaml
# ─────────────────────────────────────────────────────────────
# PLACEHOLDER — This file is REPLACED by scripts/04-wait-and-seal.sh
# at bootstrap time with a real SealedSecret generated by kubeseal.
#
# Do NOT manually edit or apply this placeholder.
# The real sealed secret will look like:
#
# apiVersion: bitnami.com/v1alpha1
# kind: SealedSecret
# metadata:
#   name: mattermost-db-credentials
#   namespace: mattermost
# spec:
#   encryptedData:
#     DB_CONNECTION_STRING: AgB3...  (long encrypted blob)
#   template:
#     metadata:
#       name: mattermost-db-credentials
#       namespace: mattermost
#     type: Opaque
# ─────────────────────────────────────────────────────────────
# This comment-only file satisfies kustomize at lint time.
# Flux will skip it gracefully until the real file is committed.
