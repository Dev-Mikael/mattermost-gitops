# clusters/production/cluster-config.yaml
# ─────────────────────────────────────────────────────────────
# AUTO-GENERATED by scripts/03-bootstrap-flux.sh from .env values.
# This file IS safe to commit — it contains no secrets.
# Flux uses it for postBuild variable substitution in Kustomizations.
# ─────────────────────────────────────────────────────────────
apiVersion: v1
kind: ConfigMap
metadata:
  name: cluster-values
  namespace: flux-system
data:
  DOMAIN: "modumichael.me"            # Replaced by bootstrap.sh
  SERVER_IP: "1.2.3.4"               # Replaced by bootstrap.sh
  LETSENCRYPT_EMAIL: "admin@modumichael.me"  # Replaced by bootstrap.sh
  CLUSTER_NAME: "production"         # Replaced by bootstrap.sh
